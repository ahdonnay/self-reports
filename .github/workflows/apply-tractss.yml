name: Publish TRACTSS treatment checklist

on:
  push:
    branches: [main]
    paths:
      - tractss-release.tar.gz

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install TRACTSS treatment checklist
        run: |
          tar -xzf tractss-release.tar.gz
          git rm -f tractss-release.tar.gz .github/workflows/apply-tractss.yml
          git add MCS/MCStm/index.html MCS/MCStm/css/styles.css MCS/MCStm/js/app.js MCS/MCStm/data/list.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Add TRACTSS treatment checklist"
          git push
